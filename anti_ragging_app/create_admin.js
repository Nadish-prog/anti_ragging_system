const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcrypt'); const prisma = new PrismaClient(); async function main() { const hash = await bcrypt.hash('password123', 10); await prisma.users.create({ data: { full_name: 'Test Admin', email: 'testadmin@gmail.com', password_hash: hash, role_id: 1 } }); console.log('Admin created: testadmin@gmail.com / password123'); } main().catch(e => { console.error(e); process.exit(1); }).finally(async () => { await prisma.$disconnect(); });