const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const admins = await prisma.users.findMany({ where: { roles: { role_name: 'ADMIN' } }, select: { email: true, full_name: true } }); console.log(JSON.stringify(admins, null, 2)); } main().catch(e => { console.error(e); process.exit(1); }).finally(async () => { await prisma.$disconnect(); });